server:
  port: 9350

logging:
  config: classpath:logback-local.xml

elasticsearch:
  thread: 50
  username: fastcatx
  password: fastcatx
  connectionTimeout: 30000
  socketTimeout: 300000
  nodes:
    - host: localhost
      port: 9200
      scheme: http
    - host: localhost
      port: 9200
      scheme: http
spring:
  profiles:
    active: local
